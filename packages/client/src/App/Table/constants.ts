import type { CSSProperties } from "react";

export const styles: Record<string, CSSProperties> = {
    table: { overflow: "scroll", flexGrow: 1 },
    paginationEnd: {
        alignSelf: "end"
    },
    paginationCenter: {
        alignSelf: "center"
    }
};
